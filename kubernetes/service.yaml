apiVersion: v1
kind: Service
metadata:
  name: weather-app
  namespace: weather-demo
  labels:
    app: weather-app
spec:
  selector:
    app: weather-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
